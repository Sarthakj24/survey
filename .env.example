# Password for the /admin page (HTTP Basic auth — any username, this password).
ADMIN_PASSWORD=changeme

# Optional: path to the SQLite file (defaults to absences.db in the project root).
# DB_PATH=absences.db

# Optional: PORT is provided automatically by Render. For local runs uvicorn
# defaults to 8000 if you don't pass --port.
# PORT=8000
